CREATE TABLE USRTAB (
ID INT NOT NULL AUTO_INCREMENT
, NAME VARCHAR(20) NOT NULL
, PASS VARCHAR(20) NOT NULL
, ROLE VARCHAR(10) NOT NULL
, JOIN_DATE DATE
, VERSION INT NOT NULL DEFAULT 0
, UNIQUE UQ_USRTAB_1 (NAME, PASS)
, PRIMARY KEY (ID)
);